"""Zynergy imaging pipeline: capture, frame-averaging, HDR merge, debayer,
CA measurement, and provenance tooling for the microscopy workflow.

Command-line entry points are defined in pyproject.toml [project.scripts].
Library modules (prov, ca_lib) are imported by the command modules.
"""

__version__ = "0.1.0"
