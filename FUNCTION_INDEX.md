# Function Index

Generated by `function_index.py` -- do not hand-edit. Regenerate with
`python3 function_index.py` after changing a module's top-level functions
or classes, or its `# CAVEAT:` comments, then commit the result.
`assert_function_index_current` (run via `python3 function_index.py
--render-check`) fails the moment this file and a fresh regeneration
disagree, so a stale index is a build failure, not a silent drift.

Grouped by module -- an agent greps blind for a *concern*, not an
*identifier*, so a flat alphabetical list across ~22k lines would give
back names with no surrounding context. Signatures are terse (one line,
via `ast.unparse`); `# CAVEAT:` blocks below a signature are the durable
facts worth reading in full.

## annotations.py

- `def _dist(a, b):`
- `def load_annotations():`
- `def new_image_record(pixel_sha256, shape, dtype, kind, calibration_ref=None, source_sha256=None):`
- `def image_record_for(pixel_sha256, store=None):`
- `def save_mark(pixel_sha256, mark, record_defaults=None):`
- `def find_orphans(store, known_hashes):`
- `def calibration_ref_for(objective, store=None):`
- `def stored_calibration_ref(pixel_sha256, store=None):`
- `def build_distance_mark(point_a, point_b, um_per_px, now=None):`
- `def build_angle_mark(vertex, arm_a, arm_b, now=None):`
- `def build_polygon_mark(points, um_per_px, now=None):`
- `def build_ellipse_mark(boundary_points, center, axes_px, angle_deg, um_per_px, now=None):`
- `def render_check():`

## ca_lib.py

- `def sample_at(img, xs, ys):`
- `def radial_warp(channel, cx, cy, scale):`
- `def apply_ca_correction(rgb, cx, cy, m_r, m_b):`
- `def adapt_center(cx, cy, calib_shape, image_shape):`

## ca_measure.py

- `def load_rgb(path):`
- `def sha256_file(path, _buf=1 << 20):`
- `def golden(f, a, b, tol=1e-05, itmax=80):`
- `def fit_lateral_ca(rgb, edge_percentile=90.0):`
- `def format_offset_table(table):`
- `def render_ca_plot(table, m_r, m_b, out_path):`
- `def poly2_flag(offset_vs_radius, rel_tol=0.15, min_abs_px=0.5):`
- `def load_ca_calibrations():`
- `def current_ca_calibration(objective, store=None):`
- `def save_ca_calibration(objective, entry):`
- `def build_ca_calibration_entry(fit_result, objective, source_path, now=None):`
- `def export_ca_calibration(entry, out_path):`
- `def load_rgb_or_mosaic(path):`
- `def main(argv=None):`
- `def render_check():`

## calibrate.py

- `def pixel_distance(p1, p2):`
- `def compute_calibration(pixel_dist, known_distance_um):`
- `def resolve_raw_path(path):`
- `def load_mosaic_array(path):`
- `def load_green_plane(path, pattern=DEFAULT_CFA_PATTERN, which=DEFAULT_GREEN_WHICH):`
- `def stretch_to_uint8(plane, lo_pct=1.0, hi_pct=99.0):`
- `def load_calibrations():`
- `def current_calibration(objective, store=None):`
- `def save_calibration(objective, entry):`
- `def build_calibration_entry(image_path, point_a, point_b, known_distance_um, objective, target_type, focus_score, pattern=DEFAULT_CFA_PATTERN, which=DEFAULT_GREEN_WHICH, now=None):`
- `def widget_to_native(widget_point, zoom):`
- `def current_rig_config():`
- `def calibration_staleness(entry, rig_config=None):`
- `def format_staleness_suffix(reasons):`
- `def main(argv=None):`
- `def render_check():`

## camera_backend.py

- `def white_level_for_bit_depth(bit_depth, container_bits=16):`
- `def derive_lores_res(preview_res, target_pixels=LORES_RES[0] * LORES_RES[1]):`
- `@dataclass(frozen=True)`
  `class LoresFrame:`
- `@dataclass(frozen=True)`
  `class CaptureResult:`
- `class CameraBackend(abc.ABC):`
- `class FakeCamera(CameraBackend):`
- `def _resolve_sensor_profile(model):`
- `class Picamera2Camera(CameraBackend):`
  > CAVEAT: the widget class name selects the Qt binding. picamera2's previews/qt.py maps QGlPicamera2 -> PyQt5 and QGl6Picamera2 -> PyQt6 via module __getattr__; there is no auto-detection. Importing plain QGlPicamera2 here builds a PyQt5 widget under a PyQt6 QApplication, which aborts with "Must construct a QApplication before a QWidget". Do not "simplify" this alias.
- `def _lores_error_is_expected(suspend_lores: bool) -> bool:`
- `def _summarize_camera_configuration(cfg: dict) -> dict:`
- `def _assert_plain_types(value, path='capabilities'):`
- `def assert_only_camera_backend_imports_picamera2():`
- `def _sensor_profile_module_names(project_dir):`
- `def assert_only_camera_backend_imports_sensor_profiles():`
- `def _sensor_profile_dimension_pairs(project_dir):`
- `def _production_region_source(path):`
- `def assert_no_hardcoded_sensor_dimension_above_driver_layer():`
- `def _bit_depth_and_white_level_literals(project_dir):`
- `def assert_no_hardcoded_bit_depth_or_white_level_above_driver_layer():`

## debayer.py

- `def tonemap_reinhard(lin, white_point):`
- `def srgb_oetf(x):`
- `def apply_colour_gains(rgb, red_gain, blue_gain):`
- `def _luma(arr):`
- `def _apply_luma_gain(arr, new_L, old_L):`
- `def shadow_deepen(arr, pivot, softness, strength, gamma):`
- `def _unsharp(arr, radius, amount, threshold):`
- `def stage_clahe(arr, clip, tiles):`
- `def sha256_file(path, _buf=1 << 20):`
- `def load_mosaic(path):`
- `def quad_means(arr):`
- `def detect_pattern(arr, swap_rb=False):`
- `def masks_for(pattern, H, W):`
- `def _conv3(plane, kernel):`
- `def green_position(pattern, which):`
- `def extract_green(arr, pattern, which):`
- `def demosaic_bilinear(arr, pattern):`
- `def demosaic_binned(arr, pattern):`
- `def cast_like(data, ref_dtype):`
- `def write(path, data, photometric, base_prov, extra):`
- `def main():`

## export.py

- `def extract_values_from_mark(mark):`
- `def build_measurement_record(pixel_sha256, image_record, mark):`
- `def export_measurements(store=None, out_path=None):`
- `def render_check():`

## focus.py

- `def _binomial(radius):`
- `def _sep_blur(plane, kernel1d):`
- `def _laplacian(plane):`
- `def variance_of_laplacian(plane, blur_radius=1):`
- `class RollingScore:`
- `@dataclass(frozen=True)`
  `class FocusBox:`
- `def score_capture_sharpness(green_plane, box=None, blur_radius=1):`
- `@dataclass(frozen=True)`
  `class BarState:`
- `class FocusBar:`
- `@dataclass(frozen=True)`
  `class FocusState:`
- `class FocusMeter:`
- `def _bar(fill, width=24):`
- `def _dwell(meter, cam, z, frames=6):`
- `def main():`
- `def render_check():`

## frame_average.py

- `def collect_inputs(inputs):`
- `def load_frame(path):`
- `def dtype_max(dtype):`
- `def clipped_mask_for_frame(arr):`
- `def write_clipped_mask(arr, mask_path):`
- `def load_clipped_mask(mask_path):`
- `def sha256_file(path, _buf=1 << 20):`
- `def read_sidecar_meta(sidecar_dir, frame_path):`
- `def aggregate_capture_field(sidecars, raw_key, caster=lambda v: v):`
- `def capture_meta_for_science(sci_files, sidecar_dir):`
- `def _checked_load(path, want_shape, want_dtype):`
- `def average_burst(files, sigma_clip=None, report_deviation=False, label='frames', gamma=None, mask_dir=None):`
- `def flat_field(sci01, flat01, dark01):`
- `def render_check():`
- `def main():`

## function_index.py

- `def _discover_modules(root: Path) -> list[Path]:`
  > CAVEAT: never iterate a bare set() of strings straight into this generator's output. CPython randomizes string hash seeds per process by default, so a set's iteration order is stable WITHIN one run but not guaranteed to match a second process's run -- it would pass a same-process determinism check and then flap between unrelated `python3 function_index.py` invocations, which is exactly the kind of spurious failure that gets a guard disabled. Sort explicitly wherever order is observable in the output.
- `def _top_level_nodes(tree: ast.Module) -> list[ast.AST]:`
- `def _signature_lines(node: ast.AST) -> list[str]:`
  > CAVEAT: this shallow-copies the node and REPLACES clone.body with a fresh list rather than mutating node.body in place. top_level_nodes holds live references to the same nodes reused later for caveat line-range bucketing; mutating a node's body here would corrupt that bucketing (end_lineno is computed from the ORIGINAL body's extent).
- `def _harvest_caveats(lines: list[str]) -> list[tuple[int, str]]:`
  > CAVEAT: this file's own comments must never start a physical line with "# CAVEAT:" when merely describing the marker in prose (as this docstring block does, by writing it mid-line) -- the harvester matches raw text, so a literal example at the start of a line would be indexed as a real caveat attached to whatever definition follows.
- `def _owner_for_line(top_nodes: list[ast.AST], line: int) -> ast.AST | None:`
  > CAVEAT: for a decorated def/class, node.lineno is the "def"/"class" line itself, NOT the first decorator line above it -- ast puts decorator_list on the node but excludes those lines from its own lineno/end_lineno range. A "# CAVEAT:" comment placed above the decorator(s) falls outside every top-level node's range and lands in the module-level bucket instead of attaching to the decorated def. Put caveat comments after the decorator line(s), immediately above "def"/"class", or inside the body, not above the decorator.
- `def _render_module(path: Path) -> str:`
- `def generate_index_text() -> str:`
- `def assert_function_index_current() -> None:`
- `def main() -> None:`
- `def render_check() -> None:`

## gallery.py

- `def _lazy_qt_shell():`
- `def _lazy_measure():`
- `def _capture_file_prefix(cap):`
- `def _capture_base_dir(session_dir, cap):`
- `def _first_frame_paths(base_dir, prefix):`
- `def _stack_id_plane_of(cap):`
- `def list_gallery_entries(out_root=None):`
- `def capture_frame_paths(entry):`
- `def capture_has_annotation(raw_path):`
- `def known_green_hashes(out_root=None):`
- `def render_check():`

## hdr_from_session.py

- `def run_tool(name, args, cwd):`
- `def frames_for(search_dir, file_prefix, ext):`
- `def yes_no(prompt, default_no=True):`
- `def display_opts(a):`
- `def pick_capture(session, kind, index):`
- `def process(capture_dir, dark_dir, session, cap, a, ext, publish_dir=None):`
- `def archive_raws(capture_dir, ext, mode):`
- `def main():`
  > CAVEAT: during the gallery-race staging design's staging window (CHANGELOG.md's 2026-08-05 entries), this field is a PROMISE, not a description. It always names the real/final session directory -- provenance never moves -- but while a capture is staged (--capture-dir below), its bytes physically live elsewhere, not here yet. Any reader that resolves a path through this field during that window, this one included, gets a path to a file that does not exist yet. Accepted, deliberately, as the same defect class as HANDOFF.md open item 2 at smaller scope -- see the CHANGELOG entry, not engineered around here.

## hdr_merge.py

- `def load_frame(path):`
- `def dtype_max(dtype):`
- `def sha256_file(path, _buf=1 << 20):`
- `def try_read_embedded_exposure(path):`
- `def try_read_embedded_capture_meta(path):`
- `def parse_exposures(raw_pairs):`
- `def merge(exposures, white_level, black, sat_frac, norm_percentile, hash_inputs, channel_layout=None, cfa_pattern=None):`
- `def _assert_single_description_tag(path):`
- `def render_check():`
- `def main():`

## imx477.py

> CAVEAT: 12 is correct by CURRENT CONFIGURATION, not by hardware -- the same shape as the frozen-GREEN_PLANE_RES-constant bug Stage 3 fixed in qt_shell.py, correct by coincidence, wrong the moment a setting moves. get_capabilities() reports three real formats on this sensor -- SRGGB8, SRGGB10, SRGGB12 (confirmed live, camera_backend.py's own sensor_modes sweep) -- and Preferences already exposes a capture- format selector built from that exact same capability set (qt_shell.py's PreferencesDialog._capture_fmt_combo; its own render_check asserts this). That selector's chosen value is persisted (save_pref("capture_format", ...)) but not yet applied to any real capture -- camera_backend.py has no format-selection hook for a still capture today (grep confirms "capture_format" is only ever saved and loaded, never read by anything that builds a still or preview config), so this constant is not silently wrong YET. It becomes silently wrong the moment that hook is built and a user picks 8-bit or 10-bit: white_level_for_bit_depth would keep deriving from this frozen 12, nothing would recompute, nothing would raise, and the merged numbers would just shift. Fixing this properly means deriving bit depth from the CONFIGURED mode (the same way preview_crop/ still_crop already derive from preview_res/still_res rather than a constant) instead of this constant -- deliberately not done here.

- `def crop_for_size(size):`

## measure.py

- `def _read_description_json(path):`
- `def check_measurement_provenance(path):`
- `def _raw_discard_reason(path):`
- `def load_measurement_plane(path, camera=None):`
- `def current_um_per_px(objective):`
- `def fit_ellipse(points):`
- `def _conic_to_ellipse(a, b, c, d, e, f):`
- `def build_record_defaults(plane, objective):`
- `class CalibrationMissing(ValueError):`
- `def commit_measurement(plane, pixel_sha256, objective, tool, points):`
- `def format_mark_result(mark):`
- `def _provenance_dir_for(capture_dir):`
- `def resolve_capture_raw(session_dir, cap):`
- `def collect_stack_planes(captures_root):`
- `def render_check():`
- `def main():`

## pixel_hash.py

- `def pixel_sha256(array):`
- `def hash_tiff(path):`

## plane_cache.py

- `def _resolve_root(root):`
- `def plane_path(pixel_sha256, root=None):`
- `def has_cached_plane(pixel_sha256, root=None):`
- `def store_plane(plane, pixel_sha256=None, root=None):`
- `def load_cached_plane(pixel_sha256, root=None):`
- `def list_cached_hashes(root=None):`
- `def referenced_hashes(store=None):`
- `def clean_cache(referenced=None, older_than_days=None, root=None, now=None):`
- `def render_check():`

## process_wizard.py

- `def _entry_label(entry):`
- `def group_for_entry(entry):`
- `def group_for_manual_file(path):`
- `def _run_tool(name, args, cwd):`
- `def run_pipeline_for_group(group, out_dir, mode, gains=None):`
- `def run_pipeline(groups, out_dir, mode, gains=None):`
- `def new_output_dir(out_root=None):`
- `def render_check():`

## provenance.py

- `def load_profile():`
- `def save_profile(locked):`
- `def _dump_meta(path, md):`
- `def new_session_dir(root=None):`
- `def new_session_dirs(capture_root=None, provenance_root=None):`
- `def new_zstack_root_dirs(capture_root=None, provenance_root=None):`
- `def new_staging_dir(name, capture_root=None):`
- `class Session:`
- `def record_capture(session, result):`
- `def record_burst(session, kind, file_prefix, result, note=''):`
- `def record_hdr(session, sci_levels, dark_levels, note=''):`
- `def render_check():`

## publish.py

- `def create_publication_manifest(green_plane_path, green_sha256, calibration_ref, results_count=0, display_images=None, now=None):`
- `def publish_measurements(green_plane_path, calibration_ref=None, out_dir=None, include_export=True):`
- `def render_check():`

## qt_shell.py

> CAVEAT: unguarded against self._capturing, unlike _open_processing_wizard right above -- this can race the auto-process worker thread's own deletion loop (Keep RAW Images off deletes this capture's raw frames + their preview .jpgs as process()'s last step). Being modal only blocks other GUI actions; it does not block a background worker thread, which is deliberately NOT blocked by the Qt event loop (that's the whole reason processing runs on one). A user opening this mid-process can list a file here and then fail to open it moments later (TOCTOU) if the worker thread's deletion lands in between. See CHANGELOG.md's 2026-08-03 "Gallery race" investigation for the concurrency contract this function doesn't yet satisfy; no guard added here.
> CAVEAT: this only keeps a LIVE Session object in sync when the caller identifies one for this exact directory (qt_shell.py's own self._session, via _on_process_ finished). measure.py's _on_exclude_toggled is a SEPARATE disk-patch writer with the identical clobber mechanism and no access to a live Session object at all -- covering it would need a cross-module registry that does not exist. That class of defect is NOT closed by this, only the one call site above that can actually name a live Session to update. See HANDOFF.md items 8a/8c.

- `def _qt6_plugin_keys(so_path):`
- `def _qt6_platformthemes_dirs():`
- `def _ensure_loadable_platformtheme():`
- `def discover_themes(themes_root=None):`
- `def resolve_theme_qss_path(theme_name, themes_root=None):`
- `def load_theme_stylesheet(qss_path):`
- `def default_green_output_path(raw_path):`
- `def build_display_flags(args):`
- `def capture_resolution_kwargs(pref=None):`
- `def preview_resolution_kwargs(pref=None):`
- `def format_lores_config_summary(cfg):`
- `def displayed_rect(widget_w, widget_h, img_aspect):`
- `def frac_from_point(px, py, disp_rect):`
- `def move_box(box, dfx, dfy):`
- `def opposite_corner(box, fx, fy, handle=HANDLE_FRAC):`
- `def native_point_from_preview_click(px, py, disp_rect, preview_crop, still_crop, green_plane_res):`
- `def preview_click_from_native_point(nx, ny, disp_rect, preview_crop, still_crop, green_plane_res):`
- `def _live_measure_tool_hint(name):`
- `def _live_measure_point_status(tool, n):`
- `def dist_point_to_segment_px(p, a, b):`
- `def live_measure_mark_segments(mark):`
- `def lores_point_from_preview_click(px, py, disp_rect, lores_res=LORES_RES):`
- `def _live_measuring_tool_hint(name):`
- `def _live_measuring_point_status(tool, n):`
- `def live_measuring_mark_segments(mark):`
- `def live_measuring_distance_px(p0, p1):`
- `def live_measuring_angle_deg(vertex, a, b):`
- `def live_measuring_polygon_stats(points):`
- `def live_measuring_result_text(mark):`
- `def _source_without_docs_and_comments(obj):`
- `def assert_live_measuring_has_no_calibration_dependency():`
- `def assert_round_trip_preview_to_native_and_back():`
- `def assert_live_measure_freeze_uses_camera_configured_green_plane_res():`
- `def _paint(ov, rs, re, cs, ce, col, alpha=255):`
- `def _rect_outline(ov, r0, r1, c0, c1, col, t):`
- `def _draw_segment_into(ov, p0, p1, col, thickness=2):`
- `def _draw_live_measuring_into(ov, marks, pending_points, thickness=2):`
- `def _draw_bar(ov, r0, r1, c_edge, fill, col, width=10):`
- `def state_color(state):`
- `def nice_tick_step_um(fov_um, target_ticks=10):`
- `def ruler_ticks(fov_width_um, fov_height_um, target_ticks=10, major_every=5):`
- `def _draw_ruler_ticks_into(ov, x_ticks, y_ticks, col=(230, 230, 230), minor_len_frac=0.02, major_len_frac=0.05, thickness=2):`
- `def should_show_onboarding_gate(already_shown, any_calibration_exists, interactive=True):`
- `def _onboarding_session_is_interactive(no_onboarding_flag=False):`
- `def render_overlay_into(ov, box, state, line=3, ruler_ticks=None, live_measuring_marks=None, live_measuring_pending=None):`
- `def render_overlay(size, box, state, line=3, ruler_ticks=None):`
- `def overlay_signature(box, state, overlay_shape, ruler_key=None, live_measuring_key=None):`
- `def build_shutter_stops(lo_us, hi_us, tol=0.03):`
- `def shutter_stop_pos(us, stops):`
- `def pos_to_shutter_stop(pos, stops):`
- `def fmt_shutter_fraction(us):`
- `def linear_to_pos(value, lo, hi, steps=GAIN_STEPS):`
- `def pos_to_linear(pos, lo, hi, steps=GAIN_STEPS):`
- `def fmt_shutter_ms(us):`
- `def load_prefs():`
- `def load_pref(key, default=None):`
- `def save_pref(key, value):`
- `def _provenance_dir_for(capture_dir):`
- `def list_sessions(capture_root):`
- `def load_session_json(session_dir):`
- `def processable_captures(session_json):`
- `def capture_correction_status(session_dir, session_json, cap, own_frames_dir=None):`
- `def archive_session_raws(session_dir):`
- `def main(argv=None):`
- `def render_check():`

## stacks.py

- `def is_tagged(cap: dict) -> bool:`
- `def is_active(cap: dict) -> bool:`
- `def plane_of(cap: dict):`
- `def load_session(session_dir) -> dict:`
- `def captures_of(session: dict) -> list:`
- `def group_by_stack(session_dirs, include_excluded: bool=False):`
- `class Issue:`
- `def validate_stack(stack_id: str, members: list) -> list:`
- `def validate_all(session_dirs) -> list:`
- `def ordered_planes(members: list) -> list:`
- `def output_name(stack_id: str, plane: int, suffix: str='_final.tif') -> str:`
- `def find_holder(captures: list, stack_id: str, plane: int):`
- `def slot_taken(captures: list, stack_id: str, plane: int) -> bool:`
- `def apply_tag(captures: list, position: int, stack_id: str, plane: int) -> dict:`
- `def find_tagged(captures: list, stack_id: str, plane: int):`
- `def set_exclude(cap: dict, excluded: bool) -> dict:`
- `def sharpness_relative_flag(score, best_score, rel_drop: float=0.5):`
- `def discarded_dir(session_dir) -> Path:`
- `def move_frames_to_discarded(session_dir, file_prefix: str, exts=('dng', 'tif', 'tiff', 'png', 'jpg')) -> list:`
- `def render_check():`

## test_burst_backend.py

- `def check(label, cond):`
- `def note(label):`
- `class _Done(QObject):`
- `def run_checks(cam):`
- `def main():`

## wizard_pages.py

- `def _lazy_qt_shell():`
- `def new_adhoc_dir(root=None):`
- `def next_snap_stem(counter):`
- `def _overlay_helpers():`
- `def render_check():`
